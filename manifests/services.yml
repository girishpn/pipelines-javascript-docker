apiVersion: v1
kind: Service
metadata:
    name: "aks1-d91d"
    labels:
        app: "aks1-d91d"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks1-d91d"
